import { component$ } from "@builder.io/qwik";

/**
 * this is inspired from the GH profile:
 * profile picture and navigation menu on the left,
 * and contents in the center-right.
 */
export const LandscapeLayout = component$(() => {
  return <div></div>;
});
